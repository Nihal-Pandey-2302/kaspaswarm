# Core dependencies
fastapi>=0.138.0
uvicorn[standard]>=0.29.0
websockets>=12.0
python-dotenv>=1.0.0

# Async HTTP client
httpx>=0.27.0

# Type checking and data validation
pydantic>=2.7.0

# Kaspa integration
# Official Kaspa Python SDK — Resolver (community-node auto-connect), RpcClient,
# transaction building/signing, and ScriptBuilder(covenants_enabled) + KIP-10
# introspection opcodes for the on-chain Agent Treasury Vault covenant. 2.0.x is
# required for covenant support (1.0.0 lacks it). Powers the live on-chain
# transport; the hand-rolled crypto stack remains for reference/depth.
kaspa==2.0.1

# Data handling
dataclasses-json==0.6.3
ecdsa
base58

# MCP server (expose the swarm as tools for external AI agents)
mcp>=1.28.0
