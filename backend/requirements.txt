# Core dependencies
fastapi>=0.138.0
uvicorn[standard]>=0.29.0
websockets>=12.0
python-dotenv>=1.0.0

# Async HTTP client
httpx>=0.27.0

# Type checking and data validation
pydantic>=2.7.0

# Kaspa integration
# Official Kaspa Python SDK — Resolver (community-node auto-connect), RpcClient,
# transaction building/signing, ScriptBuilder for covenants. Powers the live
# on-chain transport; the hand-rolled crypto stack remains for reference/depth.
kaspa==1.0.0

# Data handling
dataclasses-json==0.6.3
ecdsa
base58

# MCP server (expose the swarm as tools for external AI agents)
mcp>=1.28.0
